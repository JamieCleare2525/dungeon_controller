<nav class="navbar navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Dungeon Controller</a>
  <ul class="navbar-nav ml-auto">
    <% if current_user %>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
    <% else %>
        <%= link_to 'Login', new_user_session_path %>
    <% end %>
  </ul>
</nav>
